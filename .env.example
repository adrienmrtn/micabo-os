# Supabase (client-safe — anon key only, never the service role key here)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Monitoring (optional — Sentry init is a no-op if this is unset)
VITE_SENTRY_DSN=
