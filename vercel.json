{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/burn.py": {
      "maxDuration": 60,
      "includeFiles": "api/**",
      "excludeFiles": "{node_modules/**,dist/**,src/**,supabase/**,docs/**,public/**}"
    }
  }
}
